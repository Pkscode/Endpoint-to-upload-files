# Endpoint-to-upload-files

use localhost/docs to direct to the Swagger UI where you can upload the file and also save it on your server.

Use the following command line in terminal to run the file:
uvicorn main:app --reload



